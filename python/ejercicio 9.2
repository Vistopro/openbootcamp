import functools

def listaImpar(lista):
     numerosImpar=list(filter(lambda x: x%2!=0, lista))
     print(numerosImpar)
     print(functools.reduce(lambda a,b: a+b, numerosImpar))

listaImpar(list(range(100)))
