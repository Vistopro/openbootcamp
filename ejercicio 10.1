import tkinter
from tkinter import ttk

window=tkinter.Tk()

window.columnconfigure(0,weight=1)
window.columnconfigure(1,weight=3)

ejercicio_label=ttk.Label(window,text="EJERCICIO 1")
ejercicio_label.grid(column=0, row=0, sticky=tkinter.W,padx=10,pady=10)

boton1=ttk.Radiobutton(window,text="botón 1", value='1')
boton1.grid(column=0, row=1, sticky=tkinter.W,padx=10,pady=10)

boton2=ttk.Radiobutton(window,text="botón 2",value='2')
boton2.grid(column=0, row=2, sticky=tkinter.W,padx=10,pady=10)

boton3=ttk.Radiobutton(window,text="botón 3",value='3')
boton3.grid(column=0, row=3, sticky=tkinter.W,padx=10,pady=10)

botonOk=ttk.Button(window,text='Reiniciar',command=set())
botonOk.grid(column=1,row=3,padx=10,pady=10)
